{
  "name": "warehouse-management-system",
  "version": "1.0.0",
  "description": "Warehouse Management System for small import firm",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "db:init": "node config/init-db.js",
    "db:seed": "node config/seed.js",
    "setup": "npm run db:init && npm run db:seed"
  },
  "keywords": [
    "warehouse",
    "inventory",
    "management"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "2.2.5"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jest": "^29.7.0"
  }
}
